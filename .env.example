# 复制为 .env 后按需修改
# 本地私密覆盖可另建 .env.local（不会提交）

# 发版时请同步改：
#   1) package.json → version
#   2) src-tauri/tauri.conf.json → version
#   3) src-tauri/Cargo.toml → version
# Web 回退版本由 vite.config.js 从 package.json 注入，不必再写 VITE_APP_VERSION。

# 远程 update.json 地址（须 HTTPS，公网可访问）
# 示例内容：
# {
#   "version": "0.2.0",
#   "url": "https://your.host/cashbook-0.2.0.apk",
#   "notes": "修复若干问题"
# }
VITE_UPDATE_MANIFEST_URL=

# Android 签名：见 docs/android-update.md（手动配置 gen/android）
